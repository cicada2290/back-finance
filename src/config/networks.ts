export enum Networks {
  default = 'wss://s.devnet.rippletest.net:51233/',
  test = '',
  // dev = 'wss://s.devnet.rippletest.net:51233/',
  // ammDev = 'wss://amm.devnet.rippletest.net:51233/',
}

export enum NetworkIds {
  default = 2,
  main = 0,
  test = 1,
  // dev = 2
}
